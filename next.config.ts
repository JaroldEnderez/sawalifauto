import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["via.placeholder.com", "source.unsplash.com", "placehold.co"],
  }
};

export default nextConfig;
